# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: '3'

tasks:
  run:
    desc: "Run the backend process"
    cmds:
      - go run ./cmd/api
    env:
        LOG_LEVEL: -4
    silent: true
