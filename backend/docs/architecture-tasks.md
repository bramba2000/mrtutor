# Architecture review — task list

Derived from `backend/docs/architecture-review.md`. Ordered so nothing is a big-bang change; each phase should compile with tests green before moving to the next. Phases 0–2 need no package moves and are worth doing regardless of whether the Phase 5 restructure is adopted.

Checkboxes are for tracking progress in this file.

> **Reconciliation note (as of `dec0542`, 2026-08-08).** This file had drifted well behind the code: Phases 5, 6, most of 7, and 9 were fully or mostly implemented while still showing as `[ ]` here. Re-checked against the tree line by line; `[x]` = confirmed done, `[~]` = partially done (see the inline note for what's left), `[ ]` = confirmed still open. New items discovered during this pass are called out in "Defects found..." subsections rather than folded into the original numbering, matching this doc's existing convention from Phase 3.

---

## Phase 0 — Defect fixes (no structural change)

Do these first: cheap, independently reviewable, unblocked by everything else, and they keep the later package moves in Phase 3/5 as pure renames.

### 0.1 Security-relevant

- [x] **#1** `auth/service.go:34-37` — `Login` returns the store's not-found error verbatim, so an unknown username yields 404 while a wrong password yields 401 (user-enumeration oracle). The early return also skips bcrypt (timing oracle). Map not-found → `ErrInvalidCredentials` and compare against a fixed dummy hash on the miss path.
- [x] **#7** `cmd/api/http/auth.go:25-29,39-44` — session cookies set neither `Secure`, `SameSite`, nor `Path`. `Login` sets no `Expires`/`MaxAge` while `Register` sets 7 days — inconsistent lifetime for the same credential. Harden and unify both. **Reopened and actually closed in Phase 3:** this was ticked here but never implemented — `MaxAge: int(sessionCookieMaxAge)` fed a `time.Duration` (nanoseconds) to a seconds-valued field (604800000000000, not 604800), and `Secure`/`SameSite`/`Path` were absent. Phase 3 fixes the conversion (`.Seconds()`), adds `Path: "/"` and `SameSite: Lax`, and makes `Secure` a `authhttp.Config` field driven by `APP_MODE` (`Secure: cfg.AppMode == config.AppModeProd`) rather than hardcoded — dev traffic over plain HTTP still gets the cookie. Guarded by `auth/authhttp/handler_test.go`'s `TestLogin` and the integration suite.
- [ ] ~~**#9** `sqlite/auth.go:35` — op string is `"get principal by token "+usernameOrEmail`; `writeError` logs it on every rejected request, so every failed login writes the submitted username/email to the log. Remove the value from the op string.~~
- [x] **#13** `sqlite/auth.go:54` — `SessionStore.Create` passes `auth.ErrSessionNotFound` as the *notFound* sentinel for an `INSERT` (semantically backwards); a session PK collision degrades to a 500. Fix the sentinel usage.

### 0.2 Error handling and mapping

- [x] **#2** `sqlite/errors.go:17-19` — `translateSQLError` discards its own `notFound` argument, returning bare `errs.NotFound`; `ErrPrincipalNotFound`/`ErrSessionNotFound` never reach the client (404 body says `{"code":"internal",...}`). Wrap the passed sentinel, not `errs.NotFound`.
- [x] **#11** `cmd/api/http/errors.go:94-98` — `isJsonDecodingError` doesn't match `io.EOF`/`io.ErrUnexpectedEOF`, so an empty/truncated body yields 500 instead of 400.
- [ ] ~~**#14** `sqlite/errors.go:28` — fallback uses `%v` not `%w`, breaking `errors.Is`/`Unwrap` on the driver error chain.~~ 
- [ ] ~~**#15** `sqlite/db.go:56` — rollback error formatted with `%v`; use `errors.Join` to preserve `errors.Is` on both errors.~~
- [ ] **#16** `sqlite/errors.go:23` — conflict detection matches only the coarse `sqlite3.ErrConstraint`; use the extended codes (`ErrConstraintUnique`, `ErrConstraintForeignKey`) to report which constraint/field collided.
- [x] **#18** `sqlite/auth.go:73-81` — `principalFromDB` silently drops `UpdatedAt`; nothing writes the column either.
- [x] **#19** `cmd/api/http/auth.go:45` — `Register` returns 200 not 201, and `RegisterOut.Principal` is computed then discarded. Wire it into the response body and use the existing (currently unused) `created` helper. **Reopened and actually closed in Phase 3:** this was ticked here but the code still returned 204 via `noContent`, and the integration test asserted 200 — three sources (this doc, the code, the test) disagreed. Phase 3 makes this doc's original call authoritative: `Register` now returns 201 with `out.Principal` as the JSON body (`auth.Principal.PasswordHash` stays off the wire via its existing `json:"-"` tag; `RegisterOut` itself is never encoded directly, since it has no JSON tags and would otherwise put the session token in the response body next to the cookie).

### 0.3 Remaining defects, polish, and test gaps

- [x] **#8** `cmd/api/http/codec.go:15-21` — no `http.MaxBytesReader`; request bodies are unbounded. **Resolved in Phase 4** (landed after this line was last corrected to "unticked"): `httpx/maxbytes.go` adds a `MaxBytes()` middleware (`http.MaxBytesHandler`, 1MB), wired into the chain in `cmd/api/run.go`'s `httpx.NewRouter(...)` call.
- [x] **#10** `auth/service_test.go:83,93` — `mockPrincipalStore.Create` has a value receiver but mutates `m.count`; the increment is lost, so every principal gets `ID = 1` and overwrites `db[1]`. Fix the fixture and add a test that seeds two principals into one store (this is also why #2 was never caught).
- [x] **#12** `config/loaders.go:18,28` — `getDuration`/`getInt` swallow parse errors and silently return the default (e.g. `READ_POOL_SIZE=abc` → 4, no error). Closed by the Phase 2 `Config` rework: `config.Load` now records a parse or validation failure per env var into a `validation.Errors` and returns it, so a bad value fails startup (exit 2) instead of silently defaulting.
- [x] Polish, landed: `bodyDecoder`'s `Content-Type` check (415 for non-JSON); `validation.MinLength[T ~string | ~[]any]`; `cmd/api/main.go:57`'s `cancel` shadow.
- [ ] Polish, still open (this line previously claimed all of the below were done; they are not): `bodyDecoder` still ignores trailing content — no `dec.More()` check exists, and `httpx/codec_test.go`'s "Success when body has trailing content" subtest now documents that leniency as *intended* rather than a gap, so either implement the check or leave this box open on purpose; `validation.Email` still leaks the raw `mail.ParseAddress` message; `errs.Domain` still returns an unexported `*domainError` (`errs/domain.go:14`) callers can't name.
- [x] Test gaps, landed in Phase 3: handler-level unit tests now exist at `auth/authhttp/handler_test.go` (not `cmd/api/http/auth_test.go` — that path never existed on disk; it wasn't a 0-byte file, it was absent) covering cookie attributes, status codes, and error mapping against a fake `Service`; the integration suite's successful-login case now asserts the session cookie's `HttpOnly`/`Path`/`MaxAge`.
- [ ] Test gaps, still open: integration test calls handlers directly rather than through the mux — add a test that drives `/api/v1/...` through the real mux (needs a `cmd/api/routes_test.go` inside `package main`, since `RegisterRoutes` is unexported from a package no external test can import); `TestAuth` subtests share one DB with order dependence; `sqlite/db_test.go` touches disk without a `-short` gate.

**Standing rule this phase's stale-test episode implies:** a fix must update the test that documented the bug, in the same commit. `httpx`'s Content-Type gate (closing part of #8/polish) and defect #11's fix both landed without updating the tests that encoded the pre-fix behaviour, leaving `go test ./...` red across two packages until Phase 3 repaired them (`httpx/codec_test.go`'s missing `Content-Type` headers; `httpx/errors_test.go`'s two `"known gap"` cases for `io.EOF`/`io.ErrUnexpectedEOF`, which #11 had already closed).

---

## Phase 1 — `run(ctx) error` seam and server lifecycle

- [x] **#3** Split `main()` into `main()` + `run(ctx, stdout, stderr, lookupEnv) error` with real exit codes; reserve a distinct exit code for misconfiguration. Fixes: startup failures currently `return` and exit 0.
- [x] **#4** `cmd/api/http/server.go` — split bind from serve: `Run(ctx)` calls `net.Listen`, then `Serve(ctx, ln)`, so a listen error (port in use) is returned synchronously instead of only logged from a goroutine while the process keeps running serving nothing. Use `errors.Is(err, http.ErrServerClosed)`, not `==`.
- [x] **#6** Add `ReadHeaderTimeout`/`ReadTimeout`/`WriteTimeout`/`IdleTimeout` to the `http.Server` (currently all zero → Slowloris exposure). **Resolved:** `httpx.Config` (`httpx/server.go`) carries all four fields and `NewServer` sets them on the underlying `http.Server`; this line was left unticked past the point where the code already had it.
- [~] Retire the `isShuttingDown atomic.Bool` global (`main.go:18`, read in `routes.go:22`) for an injected `Readiness` value; split liveness (`/livez`) from readiness (`/readyz`). **Reopened and actually closed in Phase 3:** the `Readiness` value existed but `main.go`'s `isShuttingDown` global and `routes.go`'s `GET /health`/`healthHandler` reading it were never deleted — dead code nothing wrote to, so `/health` was unconditionally 200. Also, `run.go` mounted the `Readiness` handler at `/healthz`, not `/readyz` as stated here. Phase 3 deletes the global and `/health`, and renames the route to `/readyz`. Separately (and more severely — see the new defect below), `Readiness.Handler()`'s branch was inverted, so `/healthz` was permanently serving 503 while healthy; also fixed in Phase 3, with a new `httpx/readiness_test.go` (previously zero coverage). **Correction, verified against current code:** the "renames the route to `/readyz`" part never happened. `cmd/api/run.go` still mounts `router.Handle("/healthz", readiness.Handler())` alongside `router.Handle("/livez", httpx.Liveness())`. The global-deletion and inversion fixes are real and confirmed; the rename is not. Marked `[~]` (partially done) until `/healthz` → `/readyz` actually lands — trivial, one line in `run.go`.
- [x] Preserve: `run()` must **not** return `ctx.Err()` — a clean SIGTERM is success; returning `context.Canceled` would put every graceful shutdown into CrashLoopBackOff. The shutdown context must use `context.WithoutCancel` (already correct today at `main.go:57` — don't regress it).

---

## Phase 2 — `Config` struct

- [x] Replace the package-level vars in `config/{app,db,server,loaders}.go` with a `Config` struct assembled by `Load(lookup func(string) (string, bool)) (Config, error)`.
- [x] Accumulate parse errors into a `validation.Errors` (already satisfies `errors.Is(err, errs.Invalid)` for free) instead of silently defaulting on bad values — closes **#12**.
- [x] Add an options struct for `sqlite.Open` (replacing the direct `config.ReadPoolSize` read at `sqlite/db.go:133,140-141`) and for the HTTP server. The HTTP server options struct (`cmd/api/http/server.go:13-27`, `Config.LogLevel`) already landed in `55c5e77`, ahead of this phase; `sqlite.Open(ctx, sqlite.Options{Path, Logger, ReadPoolSize})` closes the remaining gap, defaulting `ReadPoolSize` to `DefaultReadPoolSize` (4) so `sqlite` stands alone.
- [x] Delete the package-level vars in the same commit — only two live consumers by the time this landed (`cmd/api/run.go`, `sqlite/db.go`), no compatibility shim needed.
- [x] Verify: `go list -deps ./sqlite ./cmd/api/http | grep config` returns nothing.
- [x] Bonus, closed as part of this phase: `LOG_LEVEL` now accepts slog level names (`DEBUG`/`INFO`/`WARN`/`ERROR`) as well as the numeric form `Taskfile.yml` uses, fixing the silent-Info fallback described in §4.1 point 3; `LOG_FORMAT` went from dead config to an actual `text`/`json` selector; `SHUTDOWN_HARD_TIMEOUT` (declared, never consumed) was removed rather than migrated.

---

## Phase 3 — Fix `wrap`, then rename `cmd/api/http` → `httpx`

Fix the generic before exporting it — freezing a known-broken signature into public API and changing it later is the one avoidable mistake here.

- [x] **#17** Constrain `wrap`'s type parameter so validation is compiler-enforced: `func Wrap[In Validable, Out any](...)`. If `Validate` is declared on `*T`, `Wrap[T]` must not compile. Add `WrapUnvalidated[In, Out any]` for inputs with nothing to validate (deliberately unpleasant name). `auth.LoginIn`/`RegisterIn` already have value-receiver `Validate()`, so both compile unchanged. Landed as designed; `wrapNoInput` was also exported as `WrapNoInput`, rebuilt on `WrapUnvalidated` since `struct{}` cannot satisfy `Validable` (not called out in this line originally, but Phase 7's `Logout` needs a no-input wrapper reachable from outside the package). Verified with `go vet`: a type whose `Validate` is pointer-receiver-only fails to compile against `Wrap` with `"does not satisfy Validable (method Validate has pointer receiver)"`.
- [x] `git mv cmd/api/http httpx` (package `http` → `httpx`); export `Wrap`, `BodyDecoder`, `OK`/`Created`/`NoContent`, `WriteError`, `StatusFor`. Also dropped the `httpstdlib` alias in `server_test.go` (needed only because of the old `http`/`net/http` name collision) and the `ehttp` alias at both `cmd/api` import sites.
- [x] Move `cmd/api/http/auth.go` straight to `auth/authhttp` (do this move once, not again in Phase 5). Fix `authHandler` visibility: `NewAuthHandler` currently returns an unexported type from an exported constructor, and takes `auth.Service` by concrete value (blocking fakes). Introduce an `authhttp.Service` interface (`Login`/`Register`/`Authenticate`/`Logout`) satisfied structurally by `auth.Service`. This unblocks writing `cmd/api/http/auth_test.go` (currently 0 bytes).
  - **Deviation 1:** `authhttp.Service` ships with only `Login`/`Register` — `auth.Service` has no `Authenticate`/`Logout` yet (those are Phase 7). Declaring all four now would mean nothing satisfies the interface and the `cmd/api` wiring wouldn't compile. Phase 7 adds the other two methods here and to the fake.
  - **Deviation 2:** the target file was never `cmd/api/http/auth_test.go` — that path doesn't exist on disk (not a 0-byte file, as both this doc and the review claimed; simply absent). The tests landed at `auth/authhttp/handler_test.go` instead, covering cookie attributes (including a direct regression guard for the `MaxAge` nanoseconds bug and the new `Config.Secure` flag), status codes, and error mapping against a fake `Service`.
  - **Deviation 3, forward note for Phase 5:** the review's planned `Register(*httpx.Router)` method on this type would collide with the `Register` *field* holding the handler. Phase 5 should name it `Mount(*httpx.Router)` instead.
  - Constructor renamed `NewAuthHandler` → `NewHandler` and the handler type `authHandler` → `Handler`, since `authhttp.NewAuthHandler` stutters once the package is no longer named `http`.
  - Picked up in the same commit, since the file was already being rewritten: `authhttp.Config{Secure, MaxAge}` (see the #7/#19 notes above), and `NewHandler`/`RegisterRoutes` now take it — sourced in `cmd/api/run.go` from `cfg.AppMode == config.AppModeProd`.
- [x] Update `test/integration/auth_test.go`'s import (currently aliased `handlers`). Now imports `auth/authhttp` unaliased.
- [x] Repaired the test suite, which was red on entry to this phase for reasons unrelated to the rename itself — see the "Test gaps" and "Polish" notes above (0.3) for what was actually stale and why.
- [x] Fixed in the same pass, in files this phase already touches (see the reopened items above): the readiness-handler inversion (`httpx/readiness.go`), the cookie `MaxAge`/`Secure`/`SameSite`/`Path` defects (#7), Register's status code (#19), and the dead `isShuttingDown` global / `GET /health` route.

### Defects found during Phase 3 (not in the original §6 inventory of 19)

The review's defect table (§6) enumerated 19 items; these surfaced only while doing this phase's file moves and are recorded here rather than renumbered into that table, so the "19 items" count in the review stays honest as a historical fact.

- [x] **Readiness handler inverted.** `httpx/readiness.go`'s `Handler()` read `if r.Ready() { 503 }` — it served 503 while healthy and 200 while draining, the exact opposite of a readiness probe's contract. A Kubernetes readiness probe would have pulled the pod out of rotation the instant it became healthy. Zero test coverage before this phase; `httpx/readiness_test.go` now covers both states.
- [x] **Session cookie `MaxAge` was in nanoseconds.** `int(sessionCookieMaxAge)` on a `time.Duration` constant produced `604800000000000`, not `604800` seconds — folded into the #7 fix above.

---

## Phase 4 — Middleware chain and Router (in `httpx`)

Purely additive — existing routes keep working.

- [x] Add a `Middleware func(http.Handler) http.Handler` chain, plus a thin `Router` wrapping `http.ServeMux` that adds middleware scoping (`ServeMux` has no notion of this). **Resolved:** `httpx/middleware.go` (`Middleware`, `Chain`) and `httpx/router.go` (`Router`, `Group`, `Use`).
- [~] Middleware set: `RequestID`, `Recover` (must re-panic on `http.ErrAbortHandler`), `AccessLog` (currently successful requests are never logged — only `writeError` logs), `Timeout` (per-request context deadline), body-size limit via `MaxBytesReader` (closes **#8**), `CORS`. **Landed:** `httpx/requestid.go`, `httpx/recover.go`, `httpx/accesslog.go`, `httpx/maxbytes.go` (closing #8) — all wired into the chain in `cmd/api/run.go`. **Still missing:** no `Timeout` middleware (no per-request context deadline anywhere in `httpx`) and no `CORS` middleware at all — `grep -ri cors` over the tree returns nothing.
- [x] Order is load-bearing and outermost-first: `RequestID` → `AccessLog` → `Recover`, so the log carries the request ID and a panic is still recorded with the 500 it produced. **Resolved:** `cmd/api/run.go` constructs the router with `httpx.RequestID(), httpx.AccessLog(logger), httpx.Recover(logger), httpx.MaxBytes()`, matching this order.
- [x] `Router.Group` must `slices.Clone` the middleware slice — without it, two sibling groups appending to a chain with spare capacity write into the same backing array, and one group silently inherits another's auth middleware (a security bug, and the most common defect in hand-rolled Go routers). **Resolved:** `httpx/router.go`'s `Group` does `mws: append(slices.Clone(r.mws), mws...)`.
- [ ] Any `ResponseWriter` wrapper for status capture must implement `Unwrap() http.ResponseWriter`, or it hides `http.Flusher`/`http.Hijacker` from handlers. **Still open — found unimplemented:** `httpx/accesslog.go`'s `recorder` type (embeds `http.ResponseWriter`, overrides `WriteHeader`) has no `Unwrap()` method. Any handler downstream of `AccessLog` that type-asserts for `http.Flusher` or `http.Hijacker` on its `ResponseWriter` will fail the assertion. No handler needs either today, so it's latent, not yet triggered.

### Defect found and fixed outside this phase's own scope

- [x] **`Recover` ran its error-mapping branch on every request, panic or not.** `httpx/recover.go:14` read `if rec := recover(); r != nil` — testing the `*http.Request` (always non-nil), not the recovered value. On a normal request this silently attempted to overwrite an already-committed response with a spurious "unknown panic" 500 (visible only as an access-log status mismatch and an "http: superfluous response.WriteHeader" debug line, since the real bytes were already on the wire) and logged a fake "panic recovered" on every single request. First observed live while manually verifying Phase 9's scheduler (an unrelated `curl /livez`), flagged there as out of scope, and fixed here as its own change. Fixed by comparing `rec != nil`; `httpx/recover_test.go` (previously nonexistent — this middleware had zero test coverage) pins the no-panic path alongside string/error/other panic values and the `http.ErrAbortHandler` re-panic.

---

## Phase 5 — Feature-first restructure

Relocate the auth adapter out of `sqlite/`; reduce `sqlite/` to an infra leaf. Depends on Phase 3 (needs `auth/authhttp` to already exist).

**Status: done**, with naming that diverges slightly from this section's original wording (noted per line below). Landed across `c98b2fe` (move + transactional registration) and later polish; not reflected as checked here until now.

- [x] Add a per-feature block to `sqlc.yml` (schema stays central in `sqlite/migrations/*.sql` — goose needs one global version sequence); move queries to `auth/authsqlite/queries/*.sql`; generate into `auth/authsqlite/internal/gen` with `omit_unused_structs: true` (also eliminates the current duplicate `Principal`/`User` structs). Delete `sqlite/internal` once migrated. **Done** — `sqlc.yml` has the per-feature block, `sqlite/internal` no longer exists on disk, generated code lives at `auth/authsqlite/internal/gen`.
- [x] Move `sqlite/auth.go` → `auth/authsqlite/store.go`. Store constructors take a `sqlite.Conn` (see Phase 6) rather than `*sqlite.DB` directly. **Done, different shape than described:** split into `auth/authsqlite/principal.go` and `auth/authsqlite/session.go` (two files, not one `store.go`); `auth/authsqlite/build.go`'s `Build(db *sqlite.DB)` constructs the non-transactional stores directly from `db.R`/`db.W`, and separately builds transactional stores from the `*sql.Tx` inside `sqlite.BuildUow`'s callback (see Phase 6 — there is no standalone `sqlite.Conn` type; the transactional path passes a raw `*sql.Tx` used as both `r` and `w`).
- [x] Move `sqlite/errors.go` → `sqlite/sqlerr.go` and **export** `translateSQLError` as `sqlite.Translate` — every feature's adapter needs it, so it stays shared (the one deliberate hub-like exception). **Done, different name:** the file stayed `sqlite/errors.go` (not renamed to `sqlerr.go`) and the exported function is `sqlite.TranslateSQLError`, not `sqlite.Translate`. Behavior matches the intent.
- [x] Add `sqlite/tx.go` with a `Handle` interface shaped identically to sqlc's `DBTX` (`ExecContext`/`PrepareContext`/`QueryContext`/`QueryRowContext`) so `sqlite` never imports any generated `gen` package — features pass their `Conn` straight to their own `gen.New`. **Done, different name:** landed as `sqlite/uow.go`'s generic `uow[Stores]`/`BuildUow`, not a standalone `Handle` interface in a `tx.go` file — `authsqlite` passes the bare `*sql.Tx` (which structurally satisfies `gen.DBTX`) straight into its own `gen.New`, achieving the same "sqlite imports no generated code" property without a named `Handle` type.
- [x] `authhttp`'s `Service` interface already landed in Phase 3 (with only `Login`/`Register`; Phase 7 adds `Authenticate`/`Logout`). Add a `Mount(*httpx.Router)` method — named `Mount`, not `Register`, because `authhttp.Handler` already has a `Register` field holding the register handler and the two would collide. **Done** — `authhttp.Handler.Mount(*httpx.Router)` exists and is called from `cmd/api/routes.go`.
- [x] Gate: `go list -deps ./sqlite | grep mrtutor` returns nothing — `sqlite` must not import any domain package. **Passes**, with a caveat on the literal command: `go list -deps ./sqlite` includes the `sqlite` package itself in its output, and that self-reference does contain `mrtutor` (`github.com/bramba2000/mrtutor/backend/sqlite`), so the grep is never truly empty. Verified instead by inspecting the full dependency list: the only match is `sqlite` itself — no other domain package appears. `sqlite` is a leaf.

---

## Phase 6 — Transaction-capable stores and `UnitOfWork`

Fixes the live data-integrity bug (**#5**): `Register` creates the principal then the session in two separate transactions, so a session-insert failure leaves an orphaned account that can't be retried.

**Status: mechanism done, this phase's own proof-of-work test is still missing.** Landed in `c98b2fe` (`feat(auth): move sqlite storage into auth/authsqlite, make registration transactional`).

- [x] `sqlite.Conn{W, R Handle}` — inside a transaction **both** fields must point at the same `*sql.Tx` (not `W=tx`/`R=db.R`), otherwise a read-your-own-write inside the transaction silently returns stale data, since `db.R` is a physically different WAL connection. This is the critical correctness point — make the correct handle the only reachable one. **Done, no separate `Conn` type:** there is no standalone `sqlite.Conn{W, R}` struct. Instead `auth/authsqlite/build.go`'s `Build` passes the *same* `*sql.Tx` as both `r` and `w` when constructing `PrincipalStore`/`SessionStore` inside `sqlite.BuildUow`'s callback (`stores.Principal = &PrincipalStore{r: tx, w: tx}`), which achieves the same correctness property — a transactional read always goes through the transaction, never `db.R` — by construction rather than via a named type.
- [x] Rebuild `authsqlite` stores from a `Conn` (constructor change from Phase 5). **Done** (see above — rebuilt from the `*sql.Tx` directly).
- [x] Add `authsqlite.UnitOfWork` implementing `auth.UnitOfWork.RunInTx` by calling `db.InTx` and rebuilding stores against the transaction's `Conn`. **Done, different plumbing:** it does not call the pre-existing `sqlite.DB.InTx`. Instead `sqlite/uow.go` adds a new generic `BuildUow[Stores any](db *DB, buildStores func(*sql.Tx) Stores) *uow[Stores]`, and `authsqlite.Build` calls it with a closure that rebuilds `auth.Stores` from the transaction. Same effect, new mechanism. The now-redundant `sqlite.DB.InTx` has since been deleted (see the "Defects found..." section below) rather than left dead.
- [x] `auth.Stores` gains a `Sessions` field (currently only has `Principals`, so it can't even express the `Register` use case). **Done** — `auth/storage.go`'s `Stores` now has both `Principal` and `Session` fields (named singular, not `Principals`/`Sessions` as this line guessed).
- [x] Update `auth.Service.Register` to use `RunInTx`, wrapping both the principal `Create` and session `Create` in one transaction. Hash the password and mint the session token **before** opening the transaction — bcrypt is ~60ms of CPU and the write pool holds exactly one connection, so hashing inside the transaction would block every other writer for that whole time. **Done** — `auth/service.go`'s `Register` calls `bcrypt.GenerateFromPassword` and `newSessionToken`/`sha256.Sum256` before calling `svc.unitOfWork.RunInTx`, exactly as specified.
- [x] Do **not** route `Login` through the `UnitOfWork` — it's a read plus one single-statement insert; wrapping it would serialize all logins behind the single writer for no atomicity gain. **Confirmed:** `Login` still calls `svc.sessionStore.Create` directly, not through `unitOfWork`.
- [x] Regression test (the proof this phase worked): force the session insert to fail (e.g. FK violation via a bogus user id) and assert the principal/`users` row does **not** survive. This test fails against today's code. **Done.** `auth/authsqlite/build_test.go`'s `TestUnitOfWork_RunInTx_RollsBackOnFailure` runs against the real `sqlite`/`authsqlite` stack: creates a principal inside `RunInTx`, then forces the session insert to fail via an FK violation (a bogus `UserID`), and asserts the principal does not survive via a fresh `PrincipalStore.GetByUsernameOrEmail` returning `ErrPrincipalNotFound`. It exercises `sqlite.BuildUow`/`authsqlite` directly rather than through `auth.Service.Register`, since `Register`'s session token is randomly generated and can't be forced to collide — this targets the transactional property `Register` depends on instead.

---

## Phase 7 — Session authentication

Auth is currently half-built: sessions are minted and never verified. `SessionStore` has only `Create`; there is no logout, no server-side expiry, no way to authenticate a request.

**Status: mostly done.** Landed across `3e0cff4`, `b96d680`, `fe44ada`, `3ab11ef`. One item (the `expires_at` migration) is genuinely still missing, and the cleanup task built to compensate for its absence has its own gap.

- [x] Migration `0003` adding `expires_at` (plus an index) to `sessions`, so expiry is a database invariant rather than a Go-side computation. **Done, different shape than described.** Migration `0003_session_last_seen.sql` landed, but instead of a stored `expires_at` it adds `last_seen_at` and drops `revoked_at`. Expiry is still computed rather than stored — deliberately: `auth.Service.Authenticate` rejects a session once `now >= created_at + auth.SessionMaxAge` (7d) **or** `now >= last_seen_at + auth.SessionIdleTimeout` (3h), both package-level `const` in `auth/service.go`, not env-driven. `Authenticate` throttles the `last_seen_at` write to once per `sessionTouchInterval` (5m) rather than once per request. `CleanupExpiredSessions` now derives its two cutoffs from the same two constants instead of a hardcoded, unrelated 10-day literal — the cookie's 7-day `Max-Age` and the server-side absolute cap can no longer drift apart. No new index was added: with a 3h idle window and a daily GC the table holds at most about a day of rows, so `idx_sessions_user_id` (kept for the `ON DELETE CASCADE`) is judged sufficient.
- [x] `SessionStore.GetActive` / `Revoke` / `DeleteExpired`; `PrincipalStore.GetByID`. **Done, two names differ.** `auth.SessionStore` has `Create`, `GetByID`, `Touch`, `Delete`, and a two-cutoff `DeleteExpired` — there is no separate `GetActive` (`GetByID` returns the row and `Service.Authenticate` applies the expiry policy itself), and `Revoke` was replaced by `Delete`: logout now hard-deletes the row instead of soft-revoking it, and `revoked_at` was dropped from the schema entirely (see the `expires_at` item above). `PrincipalStore.GetByID` exists as specified.
- [x] `Service.Authenticate` / `Service.Logout`; a new `ErrInvalidSession` sentinel. Every failure mode (missing session, expired, revoked, principal gone) must collapse to this one error so the endpoint is not a token oracle. **Done, resolved by removing a sentinel instead of adding one.** No `ErrInvalidSession` was introduced. Instead `ErrSessionRevoked` was deleted along with `revoked_at`/soft-revoke, so missing, expired (idle or absolute-cap), and revoked-via-logout sessions all now collapse onto the existing `ErrUnauthenticated` — closing the token-oracle gap this line warns about without adding a sentinel. The "principal gone" case still wraps and returns the underlying error via `fmt.Errorf`, but no longer interpolates the whole `Session` (including the token hash) into the message — just `session.UserID`.
- [x] `RequireSession` middleware in `authhttp`, mounted only on the routes that need it. Attach the authenticated principal to the request context under an **unexported zero-size key type** with a typed accessor — never a bare string key. **Done** — `auth/authhttp/middleware.go`'s `RequireSession`; `auth/principalCtx.go`'s `principalKey struct{}` with `WithPrincipal`/`FromContext` is exactly the unexported zero-size-key pattern called for.
- [~] Wire `RequireSession` into the routes that need it via `Router.Group` (validates the Phase 4 `Group`/`slices.Clone` design against a real consumer). **Done functionally, not via the mechanism this line specifies.** `auth/authhttp/handler.go`'s `Mount` wraps only `GET /auth/me` directly — `r.Handle("GET /auth/me", requireSession(httpx.WrapNoInput(...)))` — by calling the `requireSession` middleware value as a plain function around one handler, not through `r.Group(...)`. `POST /auth/logout` is **not** gated by `RequireSession` at all: it decodes the session cookie and discards any decode error (`session, _ := decodeSessionCookie(r)`), so calling it with no cookie still returns 204 (arguably fine for an idempotent logout, but means it isn't one of "the routes that need it" in this line's sense). Net effect: Phase 4's `Router.Group`/`slices.Clone` design still has no real consumer proving it works — `Group` is not called anywhere in the auth feature.

### Defects found while reconciling this doc against the code after Phases 5–9

Not previously recorded anywhere. Found by reading the current tree, not by any phase's own testing.

- [x] **`cmd/api/routes.go` has two route-registration functions.** Exported `RegisterRoutes(services Services, router *httpx.Router, logger *slog.Logger, authCfg authhttp.Config)` is dead — nothing calls it and its signature doesn't match how the app is actually wired. The live path is the unexported `registerRoutes(services Services, router *httpx.Router, logger *slog.Logger, cfg config.Config)`, called from `cmd/api/run.go`. Delete `RegisterRoutes` or reconcile the two. **Fixed:** `RegisterRoutes` deleted; `registerRoutes` is now the only route-registration function in the file.
- [x] **`sqlite.DB.InTx` is dead code again.** Flagged as zero-caller dead code in the original review (§4.7); Phase 6 did not repurpose it — it built a parallel `sqlite/uow.go` (`BuildUow`) instead. `InTx` still has zero callers in non-test code. **Fixed:** `sqlite.DB.InTx` deleted from `sqlite/db.go`; `sqlite/uow.go`'s `BuildUow`/`RunInTx` remains the only transaction mechanism.
- [x] **`sqlite/uow.go`'s `RunInTx` silently drops the rollback error.** `tx.Rollback()`'s return value is discarded entirely (not even logged) when the wrapped function fails. This is the same defect class as the original #15 (which was accepted as won't-fix for the now-dead `db.go` `InTx`), reintroduced in new code — worth a second look since it's in the path `Register` actually uses today, not dead code. **Fixed:** `RunInTx` now returns `errors.Join(err, tx.Rollback())`, preserving `errors.Is`/`errors.As` on both the original error and any rollback failure.
- [x] **No regression test proves login doesn't enumerate accounts.** Defect #1's fix (mapping not-found → `ErrInvalidCredentials`, comparing against a dummy hash) is implemented in `auth/service.go`, but `auth/service_test.go`'s `TestService_Login` only covers the two success paths (username, email) — no test asserts that an unknown username and a wrong password for a known user produce the same error/status/body. The integration suite has "Fail to login when wrong credentials" but no "unknown username" case to compare it against. The verification checklist's "No enumeration" item should stay unchecked until this exists. **Fixed:** `TestService_Login`'s new "Unknown token and wrong password are indistinguishable" subtest asserts both fail with the identical `auth.ErrInvalidCredentials` value; `test/integration/auth_test.go`'s new "Unknown user and wrong password are indistinguishable" subtest asserts byte-identical HTTP status and body.

---

## Phase 8 — Delivery

- [ ] CI running `go vet`, `go test -race`, `golangci-lint`, and a build.
- [ ] Multi-stage Dockerfile on a glibc base (cgo dependency from `mattn/go-sqlite3` — no `CGO_ENABLED=0` static binary; budget for a C toolchain in the build image).
- [ ] Response DTOs at the HTTP boundary; remove JSON tags from domain models (`auth/models.go` — `Session.TokenHash` is currently tagged `json:"id"`, `[32]byte` would marshal as an array of 32 ints if ever serialized).
- [~] Decide the API contract mechanism (OpenAPI spec / generated client) before a frontend lands — the repo root has an obvious empty slot for one and no contract exists yet. **The frontend landed anyway** (`207c158`, ahead of this item) **and is now wired to the backend** (Phase 10, below) with no contract mechanism chosen — there is still no OpenAPI spec or generated client, and the SPA's fetch calls are hand-written against `/api/v1` by convention only. The frontend now at least documents a hand-written convention for that boundary (`frontend/README.md` — one `types.ts`/`api.ts`/`queries.ts` per feature, mirroring Go DTOs by hand with a comment pointing at the source struct), so this item is no longer "no structure at all," just "no codegen." This item stays open.

---

## Phase 9 — Background scheduler

- [x] New `scheduler/` package, a domain-free kit sibling to `httpx`/`sqlite` — verified by `go list -deps ./scheduler | grep mrtutor` returning only `errs` and `validation`. In-process, goroutine-based: one goroutine per registered `Task`, so two runs of the same task can never overlap (skipping an overrun fire is a consequence of that loop's shape, not a policy it checks for).
- [x] `Schedule` is a pure interface, `Next(start, last time.Time) (time.Time, bool)`, so schedules are shareable values and testable as an unordered table. Built-ins: `Once`/`OnceAt` (fire exactly once), `Every` (optionally `.After(delay)`), and `Periodic.Within(Window)` — a recurring wall-clock span that expresses suspension ("every 5m, but only weekdays 09:00–18:00") declaratively, with no runtime `Suspend`/`Resume` state. Cron expression *parsing* is out of scope; the interface is shaped so `Cron(expr)` drops in later using `last` as its base and `start` as the fallback on the first call, with no change to the run loop.
- [x] A task returning an error wrapping `scheduler.ErrFatal` stops the whole `Scheduler` and propagates the error out of `Run`; a panic is always recovered, logged with its stack, and never fatal. Both are `runOnce`'s job, alongside `Task.Timeout`.
- [x] Shutdown drains in-flight runs on a live (`context.WithoutCancel`) task context for up to `Config.DrainPeriod`, and only cancels them if that elapses; the whole sequence is bounded by `Config.ShutdownTimeout`. A clean shutdown returns nil, never `ctx.Err()` — the same rule Phase 1 established for `httpx.Server`.
- [x] `cmd/api/run.go` now runs `httpx.Server` and `scheduler.Scheduler` under one `errgroup.WithContext`, promoting `golang.org/x/sync` from indirect to direct — the first place this repo needed two long-lived components to tear each other down. `cmd/api/tasks.go` mirrors `routes.go`: the one line a future task addition touches. No task is registered yet.
- [x] `config.Scheduler{Location, DrainPeriod, ShutdownTimeout}`, loaded via the existing `get` helper under a `SCHEDULER_` prefix — the one departure from this file's flat-name convention, because `DRAIN_PERIOD`/`SHUTDOWN_TIMEOUT` are already taken by `Server`'s own settings.
- [x] Tests use `testing/synctest` for deterministic timing (fires, skips, drain/cancel) — the first use of it in this repo, and still no `Clock` seam, consistent with `auth.Service`'s direct `time.Now()` calls.

### Defect found while manually verifying this phase

- [x] **First fire silently skipped in production.** `nextFireTime`'s skip-catch-up loop compared the very first computed fire time against a freshly sampled `time.Now()`. For any schedule with no initial delay, that first fire equals `start`, which is captured once at the top of `Run` — before any task goroutine is spawned. By the time a task's goroutine actually samples `now`, real wall-clock time has already ticked forward by whatever scheduling latency the runtime introduced, so `next.Before(now)` was always true and the task's legitimate first run was discarded as a bogus "previous run overran" catch-up. `testing/synctest`'s fake clock never advances during plain CPU work, so no test caught this — only running the real binary did. Fixed by never applying the skip check when `last` is still zero (nothing has run yet, so there is nothing to have overrun); regression-tested with a `ScheduleFunc` that fabricates an already-past first fire deterministically, since the real race can't be reproduced under the fake clock.
- [x] **`httpx.Server` never read `Config.DrainPeriod`.** Found while simulating SIGTERM/SIGINT against the compiled binary to double-check this phase's own SIGTERM claim below (line 212) — that claim only ever exercised the *scheduler's* drain, never the HTTP server's. `NewServer` stored `cfg.DrainPeriod` into `Server.drainPeriod` (since Phase 1) but `Serve` had zero read sites for it: `onShuttingDown()` (flip readiness to 503) and `server.Shutdown()` ran back-to-back with nothing in between, so the listener closed in the same instant a load balancer's readiness probe would have first observed the 503 — `READINESS_DRAIN_PERIOD` was dead config, and no `httpx` test ever set `DrainPeriod` to catch it. Fixed: `Serve` now waits out `drainPeriod` (listener still open, still serving 503) before calling `Shutdown`, with `ShutdownTimeout` bounding the whole drain-then-shutdown sequence — the same contract `scheduler.Scheduler` already implements (line 176, above). `NewServer` also now clamps `DrainPeriod` below `ShutdownTimeout` so a misconfigured drain can't consume the entire shutdown budget. Regression-tested in `httpx/server_test.go` ("Respects the drain period before shutting down", verified to fail against the pre-fix code with a connection-refused `EOF` instead of the expected 503).

---

## Phase 10 — Serve the SPA from the Go binary

- [x] New `web/` package, a domain-free kit sibling to `httpx`/`scheduler` (`go list -deps ./web | grep mrtutor` → nothing) — `web/embed.go` embeds `dist/` (`//go:embed all:dist`, the `all:` prefix needed so the committed `dist/.gitkeep` placeholder doesn't get skipped as a dotfile before a real build exists), `web/web.go`'s `Handler`/`handlerFS` serve it: SPA fallback to `index.html` for any unknown non-`assets/` path, `immutable` caching for content-hashed files under `assets/`, `no-cache` + ETag/304 support for `index.html`, a real 404 (never HTML) for a missing `assets/` file, and 405 with `Allow` for non-GET/HEAD. `handlerFS` is `fs.FS`-parameterised so `web/web_test.go` exercises all of this against `fstest.MapFS`, independent of whether a frontend build actually exists in the checkout.
- [x] `cmd/api/routes.go`'s new `newHandler` composes the binary's whole HTTP surface: a plain `http.ServeMux` routes `/api/` to the existing `httpx.Router` (still prefixed `/api/v1`, unchanged) and `/` to `web.Handler`. `httpx/router.go` itself is untouched — it cannot express an unprefixed route, and editing it for one caller would be the layout smell §7's scaling gate warns about. `healthz`/`livez` deliberately stay under `/api/v1`, matching current behavior; the separately-tracked `/healthz` → `/readyz` rename (Phase 0.1 note above) is still the item that would move them.
- [x] Missing frontend build (`web.Handler` returns `web.ErrNotBuilt`) is fatal in `APP_MODE=production` (`run.go` wraps and returns the error, non-zero exit) and a warning plus a 503-everywhere handler in `APP_MODE=development` — verified against the compiled binary with `web/dist` swapped for an empty placeholder in both modes.
- [x] `frontend/vite.config.ts` builds straight into `backend/web/dist` (`emptyOutDir: false`, so the committed `.gitkeep` survives a build; the root Taskfile's `frontend:build` does the `rm -rf` that keeps stale hashed assets from piling up instead) and proxies `/api` to the backend in both `server` and `preview` (`BACKEND_URL`, default `http://localhost:8080`).
- [x] **This closes §5.1's "CORS — mandatory the moment a browser frontend exists" without adding CORS.** The dev proxy and the single-binary production deployment mean the browser only ever sees one origin — the cross-origin case CORS exists to police never occurs. This also preserves the session cookie's `SameSite=Lax` (`auth/authhttp/handler.go`) as-is; a cross-origin setup would have forced `SameSite=None; Secure`, which plain-HTTP dev traffic can't satisfy. `docs/architecture-review.md` §5.1 and §7.4 updated accordingly — no `cors.go` is planned in `httpx/` anymore.
- [x] New root `Taskfile.yml` (`includes: backend: { taskfile: ./backend, dir: ./backend }`) orchestrates both halves: `install`, `dev` (backend + Vite concurrently), `build` (frontend build → embed → `go build`), `verify` (frontend lint/format → `backend:verify`). `backend/Taskfile.yml` itself is unchanged, so `task run`/`task test` from inside `backend/` still work with no frontend build present.
- [x] `backend/.gitignore` ignores `/web/dist/*` but force-includes `/web/dist/.gitkeep` (`!/web/dist/.gitkeep`) — the same committed-placeholder pattern `sqlite/migrations/.gitkeep` already establishes for a `//go:embed`-fed directory that must stay out of git.

### Defects found while building the students test suite

The `students` feature (Phase 5's second feature) shipped with almost no test coverage — `studentshttp` had zero, `service_test.go` and `studentssqlite/repository_test.go` covered only the happy path, and the suite itself was red (`TestService/Update` failed, and `studentssqlite`'s tests hard-failed under `-short`). Writing the missing coverage surfaced five production defects, all now fixed and pinned by a test:

- [x] **`studentssqlite.Repository` never called `sqlite.TranslateSQLError`**, unlike every `authsqlite` store. `GetByID` on a missing student leaked the raw `sql.ErrNoRows` (500 instead of 404); `Delete` on a missing student always returned `nil` despite the interface doc promising `ErrStudentNotFound` (`DeleteStudent` was `:exec`, so no rows-affected check was even possible). Fixed by switching `DeleteStudent` to `:execrows` and wrapping all four repository methods with `TranslateSQLError`. Pinned by `studentssqlite_test.TestStudentRepository/GetByID/Missing_id_returns_ErrNotFound` and `.../Delete/Missing_id_returns_ErrNotFound`.
- [x] **`toStudent` formatted a NULL `birth_date` as the string `"0001-01-01"`** (`s.BirthDate.Time.Format(...)` ran unconditionally). Fixed by guarding on `s.BirthDate.Valid`. Pinned by `.../Save/Empty_BirthDate_round-trips_as_empty_string`.
- [x] **A malformed `BirthDate` on `Save` escaped as a raw, unclassified `*time.ParseError`** (500 instead of 400). Fixed by wrapping it with `errs.Invalid`. Pinned by `.../Save/Malformed_BirthDate_is_rejected_as_invalid`.
- [x] **`UpdateIn.Validate()` panicked on every valid input.** `validateStudentData` returned `validation.Errors{...}.Err()`, which is a nil `error` when nothing failed; the caller then did an unchecked `.(validation.Errors)` type assertion on that nil interface. Masked in production because `PUT /{id}` used `httpx.WrapUnvalidated` and never called `Validate` at all. Fixed by having `validateStudentData` return `validation.Errors` directly so `Merge` (which already no-ops on nil) can be used instead of the assertion. Pinned by `students_test.TestService/UpdateIn.Validate/Succeeds_with_valid_input`.
- [x] **`PUT /{id}` accepted a body without validating it** — the only body-accepting route not on `httpx.Wrap`. Fixed by switching it to `httpx.Wrap`, which also depended on the panic fix above (validation now actually runs). Pinned by `studentshttp_test.TestUpdate/Fail_when_the_body_is_invalid` and, end to end, `integration.TestStudents/.../Update_with_an_invalid_body_fails_validation`.

Also folded in while touching these files: the validation field key for the birth date was `"birthday"` while every JSON tag on the struct was `birthDate` (`StudentData`, `CreateIn`, `UpdateIn`), and `Student.BirthDate` itself was tagged `json:"birthday"` — so a client field error named a key the client never sent, and the response used a different key than the request. Unified on `birthDate` everywhere; confirmed no frontend code depended on the old key. The `CreatedAt`/`ModifiedAt` tag inconsistencies (`create_at` typo, `modified_at` snake_case) were left alone — out of scope for a test-suite pass, flagged here for whoever next touches the wire contract.

---

## Verification checklist (from the review, §9)

- [x] `sqlite` is a leaf: `go list -deps ./sqlite | grep mrtutor` → nothing. **Passes, with a caveat on the literal command** (see Phase 5's note): the command as written always matches `sqlite`'s own import path, so read it as "no *other* domain package appears" — verified by inspecting the full `go list -deps ./sqlite` output, which contains only `sqlite` itself among `mrtutor` packages.
- [x] `config` is not a library dependency: `go list -deps ./sqlite ./httpx | grep config` → nothing. `httpx` now exists (Phase 3) and re-verifies clean; `go list -deps ./httpx | grep mrtutor` returns only `errs` and `validation` — `httpx` is domain-free.
- [ ] Failures are visible: `DATABASE_FILE=/nonexistent/x.db go run ./cmd/api; echo $?` → non-zero. **Not re-verified against current code** while reconciling this doc — `run`'s error-wrapping structure (`return fmt.Errorf("open database: %w", err)`) still looks correct by inspection, but no one has run the actual command since Phase 1.
- [ ] Port conflict is fatal: run two instances on the same port — the second exits non-zero promptly. **Not re-verified** for the same reason; `httpx.Server.Run`'s synchronous `net.Listen` still looks correct by inspection.
- [x] No enumeration: login with an unknown user and with a wrong password return byte-identical status and body. **Resolved.** `auth/service_test.go`'s "Unknown token and wrong password are indistinguishable" proves the error identity at the service level; `test/integration/auth_test.go`'s "Unknown user and wrong password are indistinguishable" proves byte-identical HTTP status and body.
- [x] `Register` is atomic: with the session insert forced to fail, no `users` row survives. **Resolved.** `auth/authsqlite/build_test.go`'s `TestUnitOfWork_RunInTx_RollsBackOnFailure` — see Phase 6's note for what it exercises and why it targets the storage layer directly rather than `Service.Register`.
- [ ] Session lifecycle: register → call an authenticated route with the cookie → logout → same call now 401. **Still open — close but not exact.** `test/integration/auth_test.go`'s `TestAuth` has "Successful logout when authenticated" (revokes a session it creates) and "Fail to get current principal when not authenticated" (no cookie at all), but no subtest reuses the *same* session token across both steps — nothing asserts that the specific session just logged out then gets 401 on `/auth/me`. Close the gap by chaining those two subtests on one token instead of two independent ones.
- [x] Routing is covered: at least one test drives the real mux through `/api/v1/...` rather than calling a handler directly. **Resolved.** `cmd/api/routes_test.go`'s `TestRegisterRoutes_RealMux` drives `registerRoutes` on the actual `/api/v1`-prefixed router; `TestNewHandler` (Phase 10) goes further and drives the real `newHandler` composition `run.go` actually calls — API routes, an unknown API version 404ing rather than falling to the SPA, and `/` falling through to `web.Handler`.
- [x] Regression suite: `task test` and `task unit-test` green; add `-race`. **`-race` has since landed** in `task verify` (`backend/Taskfile.yml`'s `verify` task runs `go test -race ./...`). **Re-verified while building the students test suite**: `task test`/`task unit-test` were not actually green going into that work — `features/students`'s service test failed (a stale in-memory fake didn't match the `Save` upsert semantics) and `studentssqlite`'s repository test hard-failed under `-short` (missing skip guard around `sqlitetest.OpenTemp`, which `t.Fatalf`s rather than skips). Both fixed. The same missing-guard pattern was then found in `cmd/api/routes_test.go` (`TestRegisterRoutes_RealMux`, `TestNewHandler`) and `features/auth/authsqlite` (`TestUnitOfWork_RunInTx_RollsBackOnFailure`, all four `TestSessionStore_*`) — all six called `sqlitetest.OpenTemp` with no `testing.Short()` check, so `task unit-test` failed on them too. Fixed the same way: a `skipIfShort(t testing.TB)` helper (one added locally in each of the two packages) skips before `OpenTemp` runs. `task test`, `task unit-test`, and `go test -race ./...` are now all green across the whole module.
- [x] Scaling smoke test: adding a second feature touches only its own new directories, a new migration, one `sqlc.yml` block, and one line in `app.go`. If it requires editing `sqlite/`, `httpx/`, or a shared routes file, the layout failed and should be revisited before feature three. **`students` is that second feature** (Phase 5's restructure was validated by it, and this pass added its missing test suite). It touched only `features/students/**`, one `sqlite/migrations/*.sql` file, and one `sqlc.yml` block — no edits to `sqlite/`, `httpx/`, or a shared routes file were needed. The layout held.
- [x] `scheduler` is domain-free: `go list -deps ./scheduler | grep mrtutor` → only `errs` and `validation`.
- [x] SIGTERM with a job in flight drains it and exits 0; a task returning `scheduler.ErrFatal` tears the HTTP server down too and exits 1 (verified against the compiled binary, not `go run`, whose own signal handling isn't representative — see Phase 9). **Caveat added on re-verification:** this only ever exercised the *scheduler's* drain — the HTTP server's own readiness drain was never actually observed under a live SIGTERM/SIGINT, and turned out to be dead code; see Phase 9's new defect entry above.
